run = "mkdir -p build && cd build && cmake .. -DCMAKE_BUILD_TYPE=Release && make -j$(nproc) && ./pillio"
entrypoint = "src/main.cpp"

[env]
API_PORT = "8080"
STORAGE_PATH = "/tmp/store.json"
FAMILY_STORAGE_PATH = "/tmp/family.json"
